# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: myfioriapptan
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://stsrvr.mynetgear.com:8030
          client: "800"
        app:
          name: ZZ1_ABAPONHANA
          description: ordresapp
          package: $tmp
          transport: ""
        exclude:
          - /test/
