#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@ui5/uiveri5/bin/uiveri5" "$@"
else 
  exec node  "$basedir/../@ui5/uiveri5/bin/uiveri5" "$@"
fi
